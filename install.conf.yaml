- defaults:
    link:
        create: true
        relink: true

- clean: []

- shell:
    - [git submodule update --init --recursive, Installing submodules]

- link:
    ~/.config/kitty: kitty
    ~/.config/starship.toml: zsh/starship.toml
    ~/.zshrc: zsh/zshrc

- shell: []
